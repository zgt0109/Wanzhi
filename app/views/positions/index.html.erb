<%= link_to '新增职位', new_position_path,remote: true, class: 'ui button positive' %>
  <table class="ui celled table">
    <thead>
       <tr>
          <th>职位名称</th>
          <th>所属部门</th>
          <th colspan="2">操作</th>
       </tr>
    </thead>
    <tbody>
    <%= render partial: "position", collection: @positions %>
    </tbody>
  </table>
<%= link_to '返回部门管理', departments_path, class: 'ui button green'%>
<%= link_to '返回员工管理', employees_path, class: 'ui button green'%>
  <!-- 弹出框 -->
   <div class="ui modal" id="newPosition">
    <i class="close icon"></i>
    <div class="header">新增职位</div>
    <div class="content"></div>
   </div>

   <div class="ui modal" id="editPosition">
    <i class="close icon"></i>
    <div class="header">修改职位</div>
    <div class="content"></div>
   </div>